---
platform: linux

inputs:
- name: tile-src
- name: wf-nozzle-version

outputs:
- name: baked-tile

run:
  path: /bin/bash
  args:
  - -ceuf
  - |
    export root_dir=$(pwd)
    version=$(cat tas2to-version/version)
    ${root_dir}/build.sh --final

